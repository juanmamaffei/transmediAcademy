%h1 Listing user_contents

%table
  %thead
    %tr
      %th Markdowncontent
      %th Type
      %th Sticked
      %th Claps
      %th Responseto
      %th User
      %th Content
      %th
      %th
      %th

  %tbody
    - @user_contents.each do |user_content|
      %tr
        %td= user_content.markdownContent
        %td= user_content.content_type
        %td= user_content.sticked
        %td= user_content.claps
        %td= user_content.responseTo
        %td= user_content.user
        %td= user_content.content
        %td= link_to 'Show', content_user_content_path(@content, user_content)
        %td= link_to 'Edit', edit_content_user_content_path(@content,user_content)
        %td= link_to 'Destroy', [@content,user_content], method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New User content', new_content_user_content_path(@content)
