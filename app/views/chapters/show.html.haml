%div{:class=> "bg-gradient-to-r from-pink-300 via-purple-300 to-indigo-400"}
  .container.mx-auto.py-12.px-8
    .py-6.text-gray-800
      .text-3xl.hover:text-green-800= link_to(@chapter.course.name, course_chapters_path(@course))
      .text-5xl= @chapter.name
      .text-lg= @chapter.description
    .text-base.text-xl.text-gray-800 Después de este capítulo, podrás:
    %ul.list-disc.px-6.text-gray-800
      -@chapter.goals.each do |obj|
        = obj
    .text-base.text-xl Índice de contenidos del capítulo
    -@contents.each do |content|
      .text-gray-800.hover:text-green-800
        %a{:href => "#" + content.sequence.to_s}
          =content.sequence
          %span |
          =content.name
      -# .absolute.right-0.top-16.hidden.lg-inset-y-0.lg:block{:class=> "lg:w-1/3"}
      -#  =image_tag "viral.svg", :class => "lg:h-full"
    = link_to 'Edit', edit_course_chapter_path(@course,@chapter)
    \|
    = link_to 'Back', course_chapters_path
- @contents.each do |content|
  - @content = content
  %a{:name => content.sequence}
  = render template: 'contents/show'